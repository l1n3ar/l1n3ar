import {
  Home, FolderKanban, MessageSquare, Quote, Code2, Music2, Activity, GitBranch, Lock, type LucideIcon,
} from 'lucide-react';
import type { NavIconName } from '@/lib/types';

export const NAV_ICONS: Record<NavIconName, LucideIcon> = {
  Home, FolderKanban, MessageSquare, Quote, Code2, Music2, Activity, GitBranch, Lock,
};
